name: Build RedSisPos-HelpDesk App

on:
  workflow_dispatch:
  push:
    branches:
      - master

jobs:
  run-ci:
    uses: ./.github/workflows/flutter-build.yml
    secrets: inherit
    with:
      upload-artifact: true
      upload-tag: "nightly"
